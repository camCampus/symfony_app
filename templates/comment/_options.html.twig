
{% block option %}
    <div class="w-full h-fit flex justify-end">
        <a class="p-1" href="{{ path('app_comment_delete', {'id': comment.id}) }}">Delete</a>
        <a class="p-1" href="{{ path('app_comment_edit', {'id': comment.id}) }}">Edit</a>
    </div>
{% endblock %}